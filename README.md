# AA-MAS Lecture Codes

You can run these codes using Jupyter Notebooks as you normally do with python. However you need to add the Julia kernel as described [here](https://datatofish.com/add-julia-to-jupyter/).